[project]
name = "frontend2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dash-svg>=0.0.12",
    "dash[async]>=4.1.0",
    "httpx>=0.28.1",
]
